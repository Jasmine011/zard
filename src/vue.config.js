module.exports = {
    //...
    //使用npm run build 打包时不生成map文件
    productionSourceMap: false
        /* devServer: {
            proxy: {
                '/api': {
                    target: 'http://39.98.123.211',
                    //pathRewrite: {'^/api' : ''}
                }
            }
        } */
};